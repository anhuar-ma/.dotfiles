-- lua/plugins/markdown/markview.lua
-- markview.nvim: in-editor markdown rendering. Migrated from the previous
-- config.

return {
  {
    "OXY2DEV/markview.nvim",
    lazy = false,
    -- Completion for blink.cmp (LazyVim's default completion engine) can be
    -- enabled by adding "saghen/blink.cmp" as a dependency if desired.
  },
}
